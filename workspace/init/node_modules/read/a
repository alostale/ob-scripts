diff -r 6841d19121bc init/fetch-modules
--- a/init/fetch-modules	Wed Oct 02 09:58:17 2013 +0200
+++ b/init/fetch-modules	Mon Oct 07 12:01:17 2013 +0200
@@ -2,7 +2,7 @@
 
 var http = require('https'),
   fs = require('fs'),
-  read = require('read'),
+ // read = require('read'),
   cacheFileDir = process.env['HOME']+'/.ob-init',
   cacheFileName = cacheFileDir+'/modules',
   file;
@@ -146,6 +146,8 @@
   fs.mkdirSync(cacheFileDir);
 }
 
+
+/*
   read({
     prompt: 'User: '
   }, function (er, u) {
@@ -161,4 +163,6 @@
       processRepo('pmods', credentials, file);
     });
   });
- 
+ */
+       processRepo('mods', {user: 'xxx', password='yyy'}, file);
+      processRepo('pmods', {user: 'xxx', password='yyy'}, file);
